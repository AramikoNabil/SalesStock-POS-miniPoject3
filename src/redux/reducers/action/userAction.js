// Action Creators

export const setUser = (payload) => ({type: 'SET_USER', payload});
export const setHome = (payload) => ({type: 'HOME_USER', payload});

export const logUserOut = () => ({type: 'LOG_OUT'});
